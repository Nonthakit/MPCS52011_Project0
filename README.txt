# Compiling

To compile the source code, make sure you have GNU compiler in your environment.
Then run `gcc comments_cleaner.c -o comments_cleaner`

# Usage

To use the program run command:

`./comments_cleaner <file_path>`

`file_path` has to have .in extension. The output will be the same filename with
.out extension within the same directory as input file.

